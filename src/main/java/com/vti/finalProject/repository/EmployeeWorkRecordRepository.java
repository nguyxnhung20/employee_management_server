package com.vti.finalProject.repository;

import com.vti.finalProject.entity.EmployeeWorkRecord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EmployeeWorkRecordRepository extends JpaRepository<EmployeeWorkRecord, Long> {
}
